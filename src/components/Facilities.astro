---
import { LayoutGrid, FlaskConical, Library, Trophy, Palette, Droplets } from "lucide-astro";
import { SCHOOL } from "../data/school";

const iconMap: Record<string, typeof LayoutGrid> = { LayoutGrid, FlaskConical, Library, Trophy, Palette, Droplets };
---

<section id="facilities" class="relative py-16 lg:py-24 bg-white overflow-hidden">
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-2xl mx-auto mb-10 lg:mb-14">
      <div data-reveal="fade" class="inline-flex items-center gap-2 px-3 py-1 bg-deep-green/10 text-deep-green rounded-full text-xs font-semibold uppercase tracking-wider mb-4">
        <span class="w-1.5 h-1.5 rounded-full bg-deep-green"></span>
        Campus
      </div>
      <h2 data-reveal class="text-3xl sm:text-4xl lg:text-5xl font-bold text-charcoal mb-4 tracking-tight">
        School <span class="text-gradient-green">Facilities</span>
      </h2>
      <p data-reveal class="text-slate text-base sm:text-lg leading-relaxed">
        {SCHOOL.facilities.subtitle}
      </p>
    </div>

    <div data-reveal-group class="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 lg:gap-6">
      {SCHOOL.facilities.items.map((item) => {
        const Icon = iconMap[item.icon] || LayoutGrid;
        return (
          <article
            data-hover-card
            class="card-facility card-sweep focus-visible:outline-deep-green relative group"
            tabindex="0"
          >
            <div data-card-inner class="icon-box" style="--inner-delay: 0ms">
              <Icon class="w-6 h-6" />
            </div>
            <div class="flex-1">
              <h3 data-card-inner class="font-bold font-serif text-charcoal mb-1 transition-colors duration-300 group-hover:text-deep-green" style="--inner-delay: 80ms">{item.title}</h3>
              <p data-card-inner class="text-slate text-sm leading-relaxed" style="--inner-delay: 160ms">{item.description}</p>
            </div>
          </article>
        );
      })}
    </div>
  </div>
</section>